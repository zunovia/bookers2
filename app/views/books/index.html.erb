<br>
<% if flash[:success]%>
<%= flash[:success]%>
<%end%>
<% if flash[:notice]%>
<%= flash[:notice]%>
<%end%>
<p id="notice"><%= notice %></p>
<div class="container">
  <div class="row">
    <div class="col-lg-4" style="background-color: white;">
      <h2>User Info</h2>
      <%= image_tag @user.get_profile_image(100,100) %>
       <table class="table table-striped">
          <tbody>
            <tr>
              <th>name</th>
              <th><%= @user.name %></th>
              <th></th>
            </tr>
            <tr>
              <th>introduction</th>
              <th><%= @user.introduction %></t>
          　</tr>
        </tbody>
      </table>

        <% if @user == current_user %>
          <%= link_to edit_user_path(@user), class: 'post-btn-lg' do %>
            <button style="width:340px; height:40px;" type="submit"><i class="fas fa-user-cog">
            </i></button>
        <% end %>
        <% end %>

      <br>
      <br>
      <h2>New book</h2>
        <%= form_with model: @book do |f| %>
          <h6>Title</h6>
          <%= f.text_field :title, size: "40"  %>
          <h6>Opinion</h6>
          <%= f.text_area :body, size: "43"  %><br>
          <%= f.submit 'Create Book',class: "btn btn-lg btn-primary" %>
        <% end %>
    </div>

    <div class="col-lg-8">
      <h2>Books</h2>
        <table class="table table-striped">
          <thead>
            <tr>
              <th></th>
              <th>Title</th>
              <th></th>
              <th>Opinion</th>
            </tr>
          </thead>

          <tbody>
            <% @books.each do |book| %>
              <tr>
              <td><%= link_to user_path(book.user.id) do %>
              <%= image_tag book.user.get_profile_image(100,100)  %>
              <% end %></td>
              <td><%= link_to book.title,book_path(book.id) %></td>
              <td></td>
              <td><%= book.body %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
</div>


