
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-12 col-md-3">
      <% if flash[:success]%>
      <%= flash[:success]%>
      <%end%>
      <% if @book.errors.any? %>
      <%= @book.errors.count %>
      <% flash[:alert]%> errors prohibited this book from being saved:
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
        <% end %>
        <% end %>

      <h2>User Info</h2>
      <%= image_tag @user.get_profile_image(100,100) %>
       <table class="table table-white">
          <tbody>
            <tr>
              <th>name</th>
              <th><%= @user.name %></th>
              <th></th>
            </tr>
            <tr>
              <th>introduction</th>
              <th><%= @user.introduction %></t>
          　</tr>
        </tbody>
      </table>

    <div class="row">
     <%= link_to edit_user_path(@user), class: "btn btn-outline-secondary btn-wrapper col-11 mx-auto" do %>
      <p><i class="fas fa-user-cog"></i></p><% end %>
    </div>

      <br>
      <br>
      <h2>New book</h2>
        <%= form_with model: @book do |f| %>
          <h6>Title</h6>
          <%= f.text_field :title, size: "30"  %>
          <h6>Opinion</h6>
          <%= f.text_area :body, size: "33"  %><br>
          <%= f.submit 'Create Book',class: "btn btn-lg btn-success" %>
        <% end %>
    </div>

    <div class="col-12 col-md-8 offset-md-1" style="background-color: white;">
      <h2>Books</h2>
        <table class="table table-hover table-inverse">
          <thead>
            <tr>
              <th></th>
              <th>Title</th>
              <th></th>
              <th>Opinion</th>
            </tr>
          </thead>

          <tbody>
            <% @books.each do |book| %>
              <tr>
              <td><%= link_to user_path(book.user.id) do %>
              <%= image_tag book.user.get_profile_image(100,100)  %>
              <% end %></td>
              <td><%= link_to book.title,book_path(book.id) %></td>
              <td></td>
              <td><%= book.body %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
</div>


